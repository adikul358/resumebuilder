meta {
  name: Google OAuth
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "rg1122@srmist.edu.in",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
